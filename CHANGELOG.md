# Changelog

บันทึกการเปลี่ยนแปลงที่สำคัญทั้งหมดของโปรเจกต์นี้

รูปแบบอ้างอิงจาก [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
และใช้เลขเวอร์ชันตาม [Semantic Versioning](https://semver.org/spec/v2.0.0.html)

## [0.2.0] - 2026-08-30

ไฟล์ที่ดูค้างไว้จะเล่นต่อจากจุดเดิมเมื่อเปิดใหม่
ที่เหลือเป็นเครื่องมือฝั่งพัฒนาและกระบวนการปล่อยรุ่น

### เพิ่มเข้ามา

- **เล่นต่อจากตำแหน่งเดิม** — เปิดไฟล์ที่เคยดูค้างไว้แล้วเล่นต่อจากจุดที่หยุด
  ท่อฝั่ง Rust, service และ action `recordPlayback` มีมาตั้งแต่แรกแต่ไม่มีใคร
  เรียกใช้ และ `lastPosition` ไม่เคยถูกตั้งค่า ตอนนี้ต่อสายครบแล้ว
  ตำแหน่งถูกบันทึกทุก 5 วินาทีระหว่างเล่น และตอนหยุด/สลับไฟล์/ปิดแอป
  ไม่บันทึกถ้ายังไม่ถึง 15 วินาทีแรก และนับว่าดูจบถ้าเหลือน้อยกว่า 30 วินาที
  สุดท้าย (กลับไปเริ่มใหม่แทนที่จะเล่นต่อตอนไตเติลจบ)

- `npm run release <version>` แก้เลขเวอร์ชันทั้ง 4 ไฟล์ (package.json,
  tauri.conf.json, Cargo.toml, Cargo.lock) แล้วเลื่อนหัวข้อ
  `[ยังไม่เผยแพร่]` ใน CHANGELOG เป็นเวอร์ชันพร้อมวันที่ให้ในคำสั่งเดียว
- `npm run version:check` ใน CI ทำให้ PR แดงถ้าเลขเวอร์ชันทั้ง 4 ไฟล์
  ไม่ตรงกัน — รอบ 0.1.1 มีการแก้แค่ `package.json` ที่เดียว ซึ่งจะได้ไฟล์
  ชื่อ 0.1.1 ที่ข้างในเป็นแอป 0.1.0 เพราะ Tauri ใช้ค่าจาก tauri.conf.json

### แก้ไข

- Workflow ปล่อยรุ่นใช้ `gh release upload` ซึ่งต้องมี release อยู่ก่อน
  แต่ tag ใหม่ยังไม่มี — v0.1.1 รอดมาได้เพราะสร้าง release ด้วยมือทันตอน
  job ยังบิลด์อยู่ ตอนนี้มี job `prepare` สร้าง release ให้เองถ้ายังไม่มี
  โดยดึงเนื้อหาจากหัวข้อใน CHANGELOG ที่ตรงกับ tag แล้วต่อท้ายด้วยวิธี
  เปิดครั้งแรกบน macOS จาก `.github/release-footer.md` — ไม่ต้องเขียนซ้ำสองที่
  และถ้าไม่มีหัวข้อของเวอร์ชันนั้นใน CHANGELOG job จะหยุดแทนที่จะปล่อย
  release เปล่าออกไป

## [0.1.1] - 2026-08-30

รุ่นเก็บงานเครื่องมือและเอกสาร ตัวโปรแกรมทำงานเหมือน 0.1.0
เพิ่มไฟล์ติดตั้ง Intel และแก้ไฟล์ macOS ให้เปิดได้โดยไม่ต้องมี Homebrew

### เพิ่มเข้ามา

- ตั้ง ESLint (flat config) พร้อม `typescript-eslint` และ `eslint-plugin-vue`
  แล้วต่อเข้า CI — `npm run lint` เดิมเรียก eslint ที่ไม่ได้ติดตั้งและไม่มี config
  จึงพังมาตลอด และ CONTRIBUTING ก็ไม่เคยพูดถึง lint เลย
- workflow `release.yml` บิลด์ตัวติดตั้ง macOS แล้วแนบเข้า GitHub release
  อัตโนมัติเมื่อ push tag `v*` โดยฝัง libmpv พร้อม dependency ทั้งกราฟ
  (47 dylib) เข้าไปในบันเดิลด้วย `dylibbundler` — ถ้าไม่ทำ ไบนารีจะชี้ไปที่
  path ของ Homebrew บนเครื่องที่บิลด์ แล้วเปิดไม่ขึ้นบนเครื่องคนอื่น
  พร้อมขั้นตอนตรวจที่ทำให้ build แดงถ้ามีอะไรยังชี้ออกนอกบันเดิล
- README อธิบายวิธีเปิดครั้งแรกบน macOS เพราะไฟล์ไม่ได้เซ็น Gatekeeper
  จะบล็อกและขึ้นข้อความที่ดูเหมือนแอปพัง

### แก้ไข

- **คำสั่งควบคุมการเล่นไม่คืน `Promise` อีกต่อไป** — `no-floating-promises`
  ชี้ว่ามีจุดที่เรียกแล้วไม่ `await` และไม่ `catch` ถึง 24 จุด ถ้า `invoke` พัง
  ขึ้นมาจะเงียบสนิท ซึ่งดูเหมือนปุ่มกดไม่ติด ตอนนี้ action พวกนี้คืน `void`
  และรายงาน error ของตัวเองผ่าน `dispatch()`
- ข้อความ warning ตอนหา libmpv ไม่เจอ เดิมยัดข้อความหลายบรรทัดของ pkg-config
  ลงใน directive ที่รับได้บรรทัดเดียว จึงถูกตัดกลางประโยคและไม่บอกวิธีแก้

### เปลี่ยนแปลง

- เปลี่ยนชื่อ `Modal.vue` → `BaseModal.vue` และ `Toast.vue` → `ToastStack.vue`
  ตาม Vue style guide (`vue/multi-word-component-names`)
- Intel runner เปลี่ยนจาก `macos-13` ที่ GitHub เลิกให้บริการแล้ว
  (job ค้างคิว 70 นาทีโดยไม่เคยได้ runner) เป็น `macos-15-intel`

## [0.1.0] - 2026-08-29

รุ่นแรกของ Velo — ตัวเล่นวิดีโอที่สร้างด้วย Tauri v2, Vue 3 และ libmpv

### เพิ่มเข้ามา

- เล่นวิดีโอด้วย libmpv พร้อม hardware decode (`hwdec=auto-safe`)
- เปิดไฟล์เดี่ยวหรือทั้งโฟลเดอร์ พร้อม playlist ที่มีโหมดวนซ้ำและสุ่ม
- เลือก audio track / subtitle track, เพิ่มไฟล์ซับจากภายนอก, ปรับ delay ของเสียงและซับ
- ปรับความเร็วเล่น 0.25x – 2x, ปรับเสียง, ปิดเสียง, fullscreen
- คีย์ลัด: `Space` เล่น/หยุด, `←` `→` ข้าม, `↑` `↓` เสียง, `M` ปิดเสียง, `F` fullscreen, `⌘O` เปิดไฟล์, `[` `]` ความเร็ว
- เก็บการตั้งค่าลงดิสก์และโหลดกลับตอนเปิดโปรแกรม
- ป้องกันจอดับระหว่างเล่น (IOKit power assertion)

### แก้ไข

- **เปิดไฟล์ไม่ได้เลย** — โปรเจกต์ไม่มีโฟลเดอร์ `src-tauri/capabilities/`
  ซึ่งใน Tauri v2 แปลว่าไม่มีสิทธิ์อะไรเลย ทำให้ `dialog.open()` ถูกปฏิเสธ
  และ `listen()` ของ event ทุกตัวใช้ไม่ได้ตามไปด้วย
- **สถานะการเล่นไม่เคยอัปเดต** — `wid` เป็น option ที่ตั้งได้ก่อน
  `mpv_initialize()` เท่านั้น แต่โค้ดเดิมเรียก initialize ไปก่อนแล้วค่อยตั้งค่า
  ทำให้คืน error และ event loop ของ mpv ไม่เคยสตาร์ต
- **วิดีโอไม่แสดงผลบน macOS** — window backend ของ mpv บน macOS ไม่รองรับ
  `--wid` เลย มันจะเปิดหน้าต่างของตัวเองเสมอ (ดูหัวข้อ *สถาปัตยกรรม* ด้านล่าง)
- **ฝ้าโปร่งแสงทับวิดีโอ** — `backdrop-blur`, gradient scrim และ box-shadow
  ล้วนกลายเป็นแผ่นทึบแสงคลุมภาพ เพราะวิดีโอเป็น native surface ที่อยู่หลัง
  webview ไม่ใช่เนื้อหาในหน้าเว็บ
- **เมนู dropdown ปิดเองระหว่างใช้งาน** — ระบบซ่อนแถบควบคุมอัตโนมัติ
  unmount ทั้งแถบทิ้งหลังไม่ขยับเมาส์ 2.5 วินาที พาเมนูที่เปิดค้างอยู่หายไปด้วย
- เมนู dropdown ไม่ปิดเมื่อคลิกที่อื่นหรือกด `Escape`
- แถบเลื่อนเวลาหลุดกลางคันถ้าแถบควบคุมถูกซ่อนระหว่างลาก
- พื้นหลังทึบอาจบังวิดีโอค้างถาวรถ้า event แจ้งสถานะหล่นหายไปครั้งเดียว —
  ตอนนี้ตรวจจากหลายสัญญาณ (`state`, `mediaInfo`, `duration`) แทนที่จะพึ่ง
  `state` อย่างเดียว

### เปลี่ยนแปลง

- ออกแบบหน้าตาส่วนควบคุมใหม่ทั้งหมด — แถบล่างเป็นเกาะทึบมุมมน,
  ชื่อไฟล์และปุ่มด้านบนแยกเป็น pill, แถบเวลาบางลงและขยายเมื่อ hover
  พร้อมตัวอย่างเวลาตอนเลื่อน, เวลาแสดงเป็น `ปัจจุบัน / ทั้งหมด` แบบ tabular
- ทุกพื้นผิวของ UI เป็นสีทึบล้วน ไม่มี blur, gradient หรือเงาฟุ้งที่แผ่ลงบนวิดีโอ
- ปุ่มข้ามคลิปจะจางลงเองเมื่อไม่มีคลิปก่อนหน้า/ถัดไป

### เครื่องมือและงานภายใน

- `.gitignore` ไม่ครอบฝั่ง Rust เลย ทำให้ `src-tauri/target/` (สองหมื่นกว่าไฟล์)
  จะถูกเก็บเข้า git — เพิ่ม `/src-tauri/target/` และ `/src-tauri/gen/schemas/`
- จัดรูปแบบโค้ด Rust ทั้งหมดด้วย `cargo fmt` และแก้ `cargo clippy` ให้ผ่านแบบ
  `-D warnings` ตามที่ CI บังคับ (20 ข้อ) ครอบคลุมการใช้ c-string literal,
  การรวม `if` เข้ากับ match guard, `Default` ของ struct ที่มี `new()`
  และเอกสาร `# Safety` ของฟังก์ชัน `unsafe` ที่เปิด public
- ลบ `EventLoop::new()` ที่ไม่มีใครเรียกออก
- `platform::macos` และ `platform::windows` ถูกประกาศแบบไม่มี cfg ทำให้
  `macos.rs` ถูกคอมไพล์บน Windows แล้วพังที่ `#[link(kind = "framework")]`
  ซึ่งมีเฉพาะบน Apple — gate ตาม target ทั้งสองโมดูล รวมถึง `player::render`
  ที่เรียก `dlopen`/`dlsym`
- CI: ข้าม `cargo test` บน Windows runner เพราะไม่มี libmpv ให้ link
  (ยังรัน `cargo fmt --check` และ `cargo clippy` ซึ่งไม่ต้อง link)

### สถาปัตยกรรม

- **ย้ายการแสดงผลวิดีโอบน macOS ไปใช้ mpv render API**

  เดิมส่ง NSView ให้ mpv ผ่าน `wid` ตามแผน แต่พบว่า window backend ของ mpv
  บน macOS ไม่สนใจค่านี้ และเปิด `NSWindow` ของตัวเองขึ้นมาแยกเสมอ
  (ยืนยันแล้วทั้ง `vo=gpu-next` และ `vo=gpu`)

  วิธีแก้ชั่วคราวคือดึงหน้าต่างนั้นมาเป็น child window แล้วบังคับตำแหน่งเอง
  ซึ่งต้องพึ่ง implementation ภายในของ mpv หลายจุด — ค้นหาหน้าต่างจากชื่อคลาส,
  เขียนทับ `constrainFrameRect:toScreen:`, สลับ window level ตามโหมด fullscreen,
  ดัก NSWindow notification เอง — ทั้งหมดไม่ใช่ API ที่การันตี และพังง่าย

  ตอนนี้ macOS ใช้ `vo=libmpv` แล้ววาดผ่าน `mpv_render_context` ลง
  `NSOpenGLView` ที่เป็น subview อยู่ใต้ webview ในหน้าต่างเดียวกัน ผลคือ
  fullscreen, การย่อขยาย และ retina scaling ทำงานเองโดยไม่ต้องมีโค้ดจัดการ
  และไม่มีอะไรพึ่ง internal ของ mpv อีก

  Windows ยังใช้ `vo=gpu-next` กับ `wid` เหมือนเดิม เพราะรองรับตามปกติ

### ข้อจำกัดที่ทราบ

- `backdrop-filter` (blur) ใช้กับ UI ที่ซ้อนบนวิดีโอไม่ได้ เพราะ WKWebView
  อ่านพื้นหลังที่อยู่นอกตัวมันเองไม่ได้ — ให้ใช้สีทึบแทน
- ยังทดสอบเฉพาะจอเดียว ยังไม่ได้ตรวจ multi-monitor, Split View และ Stage Manager
- ประวัติการดูและการเล่นต่อจากตำแหน่งเดิมยังไม่ทำงาน — คำสั่งฝั่ง Rust
  (`history_record`, `history_get`), service และ action `recordPlayback`
  ในสโตร์มีครบแล้ว แต่ยังไม่มีจุดไหนเรียกใช้ระหว่างเล่น และ `lastPosition`
  ของรายการใน playlist ไม่เคยถูกตั้งค่า

[0.2.0]: https://github.com/emozerorise/velo/releases/tag/v0.2.0
[0.1.1]: https://github.com/emozerorise/velo/releases/tag/v0.1.1
[0.1.0]: https://github.com/emozerorise/velo/releases/tag/v0.1.0
